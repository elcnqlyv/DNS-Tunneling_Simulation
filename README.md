# DNS Tunneling Simulation

Simple Python project that simulates sending data through DNS query names.

The sender:

1. Reads a message.
2. Converts it to hexadecimal.
3. Splits the hexadecimal string into 4-character chunks.
4. Prints each chunk as a subdomain.

Example:

```text
4865.example.com
6c6c.example.com